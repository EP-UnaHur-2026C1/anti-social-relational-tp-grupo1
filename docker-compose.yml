version: "3.8"

services:
  app:
    build: .
    container_name: anti-social-api
    ports:
      - "3000:3000"
    volumes:
      # Persistir la base de datos SQLite fuera del contenedor
      - ./data:/app/data
    environment:
      - PORT=3000
      - X_MESES=6
    restart: unless-stopped
